services:
  - type: web
    name: story-bible-navigator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./scripts/start_live_server.sh
    autoDeploy: true
